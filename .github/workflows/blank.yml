name: IREXT_ULTIMATE_SIMPLIFIED_CHINESE_BUILD

on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 1. 环境初始化
        run: |
          rm -rf *
          mkdir -p app/src/main/java/com/irext/decodesdk
          mkdir -p app/src/main/java/hap/00660/re
          mkdir -p app/src/main/res/layout
          mkdir -p app/src/main/jniLibs
          echo "✅ 兵工厂已就绪"

      - name: 2. 部署 JNI 硬件解码驱动
        run: |
          wget "https://irext-lib-release.oss-cn-hangzhou.aliyuncs.com/1.5.2/decode_sdk/libirdecode_jni_1.5.2-android.zip" -O jni.zip
          unzip jni.zip -d jni_temp
          SOURCE_DIR=$(find jni_temp -name "arm64-v8a" -type d | head -n 1 | xargs dirname)
          cp -r $SOURCE_DIR/* app/src/main/jniLibs/
          echo "✅ 硬件解码库 .so 已注入"

      - name: 3. 编写 JNI 桥接层代码
        run: |
          cat <<EOF > app/src/main/java/com/irext/decodesdk/IRDecode.java
          package com.irext.decodesdk;
          public class IRDecode {
              static { System.loadLibrary("irdecode"); }
              public native int init();
              public native int open(int category, int subCategory, byte[] bin, int binLength);
              public native int[] decode(int keyCode, int[] acStatus);
              public native void close();
          }
          EOF

      - name: 4. 编写核心服务 (带 AppKey 配置逻辑)
        run: |
          cat <<EOF > app/src/main/java/hap/00660/re/IRService.java
          package hap.00660.re;
          import android.app.*;
          import android.content.*;
          import android.hardware.ConsumerIrManager;
          import android.os.IBinder;
          import com.irext.decodesdk.IRDecode;
          import fi.iki.elonen.NanoHTTPD;
          import java.util.Map;

          public class IRService extends Service {
              private WebServer server;
              private ConsumerIrManager ir;
              private IRDecode decoder = new IRDecode();
              private SharedPreferences pref;

              @Override
              public void onCreate() {
                  super.onCreate();
                  pref = getSharedPreferences("IREXT_CONFIG", MODE_PRIVATE);
                  String ID = "IR_HAP";
                  if (android.os.Build.VERSION.SDK_INT >= 26) {
                      NotificationChannel ch = new NotificationChannel(ID, "IR", NotificationManager.IMPORTANCE_LOW);
                      getSystemService(NotificationManager.class).createNotificationChannel(ch);
                  }
                  startForeground(1, new Notification.Builder(this, ID).setContentTitle("IREXT 网关已启动").setContentText("监听端口: 8080").setSmallIcon(android.R.drawable.ic_dialog_info).build());
                  
                  ir = (ConsumerIrManager) getSystemService(CONSUMER_IR_SERVICE);
                  decoder.init();
                  try { server = new WebServer(8080); server.start(); } catch (Exception e) {}
              }

              private class WebServer extends NanoHTTPD {
                  public WebServer(int port) { super(port); }
                  @Override
                  public Response serve(IHTTPSession session) {
                      Map<String, String> p = session.getParms();
                      
                      // 1. 保存授权信息接口
                      if (p.get("save_auth") != null) {
                          pref.edit().putString("key", p.get("key")).putString("secret", p.get("secret")).apply();
                          return newFixedLengthResponse("配置已保存，正在重新认证...");
                      }

                      // 2. 发射接口
                      if (p.get("raw") != null) {
                          try {
                              String[] s = p.get("raw").split(",");
                              int[] pat = new int[s.length];
                              for(int i=0; i<s.length; i++) pat[i] = Integer.parseInt(s[i].trim());
                              ir.transmit(38000, pat);
                              return newFixedLengthResponse("已发射");
                          } catch (Exception e) { return newFixedLengthResponse("格式错误"); }
                      }

                      // 网页前端 (简体中文 + 认证输入框)
                      String html = "<html><head><meta charset='utf-8'><meta name='viewport' content='width=device-width, initial-scale=1'>" +
                          "<style>body{background:#f0f2f5;font-family:sans-serif;text-align:center;padding:15px;} " +
                          ".card{background:#fff;border-radius:15px;padding:20px;box-shadow:0 4px 15px rgba(0,0,0,0.1);margin-bottom:20px;}" +
                          "button{width:100%;padding:15px;margin:8px 0;border:none;border-radius:10px;background:#1890ff;color:white;font-size:16px;font-weight:bold;} " +
                          "input{width:100%;padding:12px;margin:10px 0;border:1px solid #ddd;border-radius:8px;}</style></head>" +
                          "<body><h2>📡 IREXT 官方终端网关</h2>" +
                          "<div class='card'><h3>1. 官方授权认证</h3>" +
                          "<input id='ak' placeholder='输入 App Key' value='"+pref.getString("key","")+"'/>" +
                          "<input id='as' placeholder='输入 App Secret' value='"+pref.getString("secret","")+"'/>" +
                          "<button onclick='save()'>保存配置并认证</button></div>" +
                          "<div class='card'><h3>2. 品牌码库检索</h3>" +
                          "<input id='bn' placeholder='搜索品牌, 如: 格力'/>" +
                          "<button onclick='alert(\"认证后将启用官方SDK检索\")'>搜索云端</button></div>" +
                          "<script>function save(){ var k=document.getElementById(\"ak\").value; var s=document.getElementById(\"as\").value;" +
                          "fetch(\"/?save_auth=1&key=\"+k+\"&secret=\"+s).then(r=>r.text()).then(alert); }</script>" +
                          "</body></html>";
                      return newFixedLengthResponse(html);
                  }
              }
              @Override public IBinder onBind(Intent intent) { return null; }
          }
          EOF

      - name: 5. 编写 MainActivity (简体中文 + Root白名单)
        run: |
          cat <<EOF > app/src/main/java/hap/00660/re/MainActivity.java
          package hap.00660.re;
          import android.app.Activity;
          import android.content.Intent;
          import android.os.Bundle;
          import android.widget.TextView;
          public class MainActivity extends Activity {
              protected void onCreate(Bundle saved) {
                  super.onCreate(saved);
                  TextView tv = new TextView(this); tv.setTextSize(18); tv.setPadding(40,40,40,40); setContentView(tv);
                  new Thread(() -> {
                      try {
                          Process p = Runtime.getRuntime().exec("su");
                          java.io.DataOutputStream os = new java.io.DataOutputStream(p.getOutputStream());
                          os.writeBytes("dumpsys deviceidle whitelist +" + getPackageName() + "\n");
                          os.writeBytes("exit\n"); os.flush();
                      } catch (Exception e) {}
                  }).start();
                  startService(new Intent(this, IRService.class));
                  tv.setText("🚀 IREXT 官方SDK网关\n\n✅ 简体中文版\n✅ 支持动态授权认证\n✅ JNI 本地解密引擎\n\n请访问 8080 端口输入 Key 认证");
              }
          }
          EOF

      - name: 6. 清单文件 (包名 hap.00660.re)
        run: |
          cat <<EOF > app/src/main/AndroidManifest.xml
          <manifest xmlns:android="http://schemas.android.com/apk/res/android" package="hap.00660.re">
              <uses-permission android:name="android.permission.TRANSMIT_IR" />
              <uses-permission android:name="android.permission.INTERNET" />
              <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
              <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
              <application android:label="HAP红外网关" android:theme="@android:style/Theme.DeviceDefault">
                  <activity android:name=".MainActivity" android:exported="true">
                      <intent-filter><action android:name="android.intent.action.MAIN" /><category android:name="android.intent.category.LAUNCHER" /></intent-filter>
                  </activity>
                  <service android:name=".IRService" android:exported="false" />
              </application>
          </manifest>
          EOF

      - name: 7. 最终 Gradle 配置 (引入官方 SDK)
        run: |
          echo "include ':app'" > settings.gradle
          cat <<EOF > build.gradle
          buildscript { repositories { google(); mavenCentral() }; dependencies { classpath 'com.android.tools.build:gradle:8.2.2' } }
          allprojects { 
              repositories { 
                  google(); mavenCentral();
                  maven { url "http://maven.irext.net/"; allowInsecureProtocol = true }
              } 
          }
          EOF
          cat <<EOF > app/build.gradle
          plugins { id 'com.android.application' }
          android {
              compileSdk 34
              namespace "hap.00660.re"
              defaultConfig { 
                  applicationId "hap.00660.re"
                  minSdk 21; targetSdk 34; ndk { abiFilters 'armeabi-v7a', 'arm64-v8a' }
              }
          }
          dependencies {
              implementation 'org.nanohttpd:nanohttpd:2.3.1'
              implementation 'net.irext.webapi:irext-androidapi:1.5.2'
          }
          EOF

      - name: 8. 执行云编译并输出 MD5
        uses: gradle/actions/setup-gradle@v4
        with: { java-version: 17, gradle-version: 8.7 }
        
      - name: 9. 编译并提取签名
        run: |
          gradle assembleDebug
          echo "========================================================="
          echo "👇 把下面这行 MD5 填入控制台：hap.00660.re 的签名栏 👇"
          keytool -list -v -keystore ~/.android/debug.keystore -alias androiddebugkey -storepass android -keypass android | grep MD5 | sed 's/MD5: //' | sed 's/://g' | tr '[:upper:]' '[:lower:]'
          echo "========================================================="

      - name: 10. 导出 APK
        uses: actions/upload-artifact@v4
        with: { name: IREXT_PRO_CHINESE_APK, path: app/build/outputs/apk/debug/app-debug.apk }
